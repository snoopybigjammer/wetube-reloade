header
    a(href="/").header__logo
        i.fab.fa-youtube
    nav
        ul
            li
                a(href="/search")
                    i.fa-solid.fa-magnifying-glass
            if loggedIn
                li
                    a(href="/users/logout") Log out
                li
                    a(href="/users/edit") Edit profile
                li
                    a(href="/videos/upload") upload vids
                li
                    a(href=`/users/${loggedInUser._id}`)
                        if loggedInUser.avatarUrl === ""
                            span 😀
                        else
                            img.header__avatar(src="/" + loggedInUser.avatarUrl)

        
                                        
            else
                li
                    a(href="/join").header__btn join us
                li
                    a(href="/login").header__btn log in  

